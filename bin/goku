#!/usr/bin/env bash
CUR_WD=`pwd`
DP="dev-packages"
if [[ -d "$DP" ]]; then
	pushd "$DP"
	for pkg in "`pwd`/*";
	do
		pushd $pkg
		git smart-pull
		popd
	done
	popd
fi

pushd ~/.kit && git pull && popd

cd $CUR_WD