#!/bin/bash
#
# Create a temp ruby file for quick scripts 
# and open in TextMate
#
TMP_FILE=`mktemp -t tmp_ruby`
RB_FILE=${TMP_FILE}.rb

mv $TMP_FILE $RB_FILE
echo '#!/usr/bin/env ruby' > $RB_FILE
chmod 744 $RB_FILE
mate -l2 $RB_FILE

